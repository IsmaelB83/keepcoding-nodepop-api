<!DOCTYPE html>
<html>
  <head>
    <title>Nodepop</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css?family=Lato&display=swap" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon.png">
  </head>
  <body>
    <div class='container'>
      <div class='row'>
        <div class='col-12 text-center mt-4 mb-2'>
          <h1>Bienvenido a Nodepop</h1>
          <a href="/">Home</a>
        </div>
        <div class='col-12'>
          <p>A continuación puede ver todos los anuncios disponibles con la búsqueda realizada</p>
        </div>
      </div>
      <div class='row'>
        <div class='col-12 anuncios mb-4'>
          <% if (locals.count && locals.count > 0) { %>
            <% for(let i=0; i<results.length; i++) { %>
              <div class="card">
                <a href="/?venta=<%=results[i].type==='sell'%>">
                  <img src="<%= results[i].type === 'buy' ? '/images/buy.png' : '/images/sell.png' %>" class="card-icon" alt="icon">
                </a>
                <img src="<%= results[i].photo %>" class="card-img-top" alt="...">
                <span class="price"><%= results[i].price.toFixed(2) %> €</span>
                <div class="card-body text-justify">
                    <h5 class="card-title text-uppercase mb-1 font-weight-bold">
                      <%= results[i].name %> 
                    </h5>
                    <p class="card-text text-black-50"><small><%= results[i].description %></small></p>
                </div>
                <div class="card-footer text-muted py-1 px-2">
                  <p class="card-text mb-1">
                    <% for(let j=0; j<results[i].tags.length; j++) { %>
                      <a href="/?tag=<%=results[i].tags[j]%>"><span class="badge badge-<%=results[i].tags[j]%>"><%=results[i].tags[j]%></span></a>
                    <% } %>
                  </p>
                  <a href="/<%=results[i].id%>" class="btn btn-block btn-primary">Detalle</a>
                  <small id="passwordHelpInline" class="text-muted">
                    Anuncio creado <%= moment(results[i].createdAt).fromNow(); %>
                  </small>
                </div>
              </div>
            <% } %>
          <% } else { %>
            <p class="font-weight-bold">No hay resultados</p>
          <% } %>
        </div>
      </div>
    </div>
    
  </body>
</html>